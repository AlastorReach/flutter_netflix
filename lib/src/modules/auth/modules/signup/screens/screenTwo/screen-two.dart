import 'package:flutter/material.dart';

class SignupScreenTwo extends StatelessWidget {
  const SignupScreenTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sign up screen two'),
      ),
    );
  }
}
