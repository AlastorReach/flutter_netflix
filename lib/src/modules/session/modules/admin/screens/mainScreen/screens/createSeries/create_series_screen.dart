import 'package:flutter/material.dart';

class CreateSeriesScreen extends StatelessWidget {
  const CreateSeriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Create series Screen'),
    );
  }
}
