import 'package:flutter/material.dart';

class UpdateeMovieScreen extends StatelessWidget {
  const UpdateeMovieScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Update movie Screen'),
    );
  }
}
